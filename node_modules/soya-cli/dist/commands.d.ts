import { Soya } from "soya-js";
import { CmdArgs } from "./utils/cmd";
declare type ParamObject = Omit<CmdArgs, 'default'> & {
    default?: string;
};
export declare const systemCommands: {
    [key: string]: (params: ParamObject, soya: Soya) => Promise<void>;
};
export {};
