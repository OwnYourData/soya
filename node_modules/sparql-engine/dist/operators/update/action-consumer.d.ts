import { Consumable } from './consumer';
/**
 * A consumer that executes a simple action
 * @author Thomas Minier
 */
export default class ActionConsumer implements Consumable {
    private _action;
    constructor(_action: () => void);
    execute(): Promise<void>;
}
