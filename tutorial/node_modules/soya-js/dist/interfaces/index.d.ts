export interface IntSoyaDocument {
    ['@context']: {
        ['@version']: number;
        ['@base']: string;
        ['@import']: string;
        [key: string]: string | number;
    };
    graph: any[];
}
export declare type SoyaDocument = Omit<IntSoyaDocument, 'graph'> & {
    '@graph': any[];
};
export declare type IntSoyaInstance = {
    "@context": {
        "@version": number;
        "@vocab": string;
    };
} & Pick<IntSoyaDocument, 'graph'>;
export declare type SoyaInstance = Omit<IntSoyaInstance, 'graph'> & {
    '@graph': any[];
};
export declare const isInstance: (document: any) => document is SoyaInstance;
